services:
  docs:
    image: remi_shop_local_docs
    container_name: remi_shop_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./remi_shop:/app/remi_shop:z
    ports:
      - '9000:9000'
    command: /start-docs
