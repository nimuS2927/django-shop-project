# PostgreSQL
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5532
POSTGRES_DB=debug
POSTGRES_USER=debug
POSTGRES_PASSWORD=debug

POSTGRES_DATA_PATH=${BASE_DIR}/postgres_data
POSTGRES_BACKUPS_PATH=${BASE_DIR}/postgres_backups

DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
